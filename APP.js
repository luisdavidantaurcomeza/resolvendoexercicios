//Mostre um alerta com a mensagem "Boas vindas ao nosso site!".

//let mensagem = 'Boas vindas ao nosso site: ';
//alert (mensagem);

// Declare uma variável chamada nome e atribua a ela o valor "Lua".

//let nome = 'lua';

//Crie uma variável chamada idade e atribua a ela o valor 25:

//let idade = 25;

//Defina uma variável numeroDeVendas e atribua a ela o valor 50.

//let numeroDeVendas = 50;

//Defina uma variável saldoDisponivel e atribua a ela o valor 1000. 

//let saldoDisponivel = 1000; 

//Exiba um alerta com o texto "Erro! Preencha todos os campos";

/* let mensagem = 'Erro! Preencha todos os campos';
alert (mensagem);  */

//Declare uma variável chamada mensagemDeErro e atribua a ela o valor "Erro! Preencha todos os campos" Agora exiba um alerta com o valor da variável mensagemDeErro.
/* let mensagemDeErro = 'Erro! Preencha todos os campos';
alert (mensagemDeErro); */

//Use um prompt para perguntar o nome do usuário e armazená-lo na variável nome.

/* let nome = prompt('Digite o seu nome');
 */

//peça ao usuário para digitar sua idade usando um prompt e armazene-a na variável idade.
/* let idade = prompt ('informe sua idade: ');  */

//Agora, caso a idade seja maior ou igual que 18, exiba um alerta com a mensagem "Pode tirar a habilitação!".

/* let idade = prompt ('informe sua idade: ');

if (idade>=18){
    alert('pode tirar sua habilitação');
} else {
    alert('ainda não é possível tirar a habilitação')
}
 */

//Pergunte ao usuário qual é o dia da semana. Se a resposta for "Sábado" ou "Domingo", mostre "Bom fim de semana!". Caso contrário, mostre "Boa semana!".
/* let diaDaSemana = prompt ('Qual o dia da semana? segunda, terça, quarta, quinta, sexta, sábado ou domingo');

switch (diaDaSemana) {
    case 'segunda'||'SEGUNDA'||'Segunda'||'SEgunda'||'SEGunda'||'SEGUnda'||'SEGUNda':
      alert ('Tenha uma boa semana! ');
        break;
    case 'terça'||'Terça'||'TErça'||'TERça'||'TERÇa'||'TERÇA':
      alert ('Tenha uma boa semana! ');
        break;
    case 'quarta'||'Quarta'||'QUarta'||'QUArta'||'QUARta'||'QUARTa'||'QUARTA':
      alert ('Tenha uma boa semana! ');
        break;
    case 'quinta'||'Quinta'||'QUinta'||'QUInta'||'QUINta'||'QUINTa'||'QUINTA':
      alert ('Tenha uma boa semana! ');  
        break;
    case 'sexta'||'Sexta'||'SExta'||'SEXta'||'SEXTa'||'SEXTA':
      alert ('Tenha uma bom fim de semana! ');
        break;
    case 'sábado'||'Sábado'||'SÁbado'||'SÁBado'||'SÁBAdo'||'SÁBADo'||'SÁBADO':
      alert ('Tenha um bom fim de semana! ') 
        break;  
    case 'domingo'||'Domingo'||'DOmingo'||'DOMingo'||'DOMIngo'||'DOMINgo'||'DOMINGo'||'DOMINGO':
      alert ('Tenha um bom fim de semana! ')     
      break;
    default:
      alert ('digite um dia válido! ')
      break;
  }  */
//Verifique se um número digitado pelo usuário é positivo ou negativo. Mostre um alerta informando.

/* let numero = prompt ('informe um numero: ');

if (numero>=0){
    alert('o número digitado é positivo!! '); 
} else  if(numero<0){
    alert('o número digitado é negativo!! ');
} else {
    alert('valor inválido ');
}
 */

//Crie um sistema de pontuação para um jogo. Se a pontuação for maior ou igual a 100, mostre "Parabéns, você venceu!". Caso contrário, mostre "Tente novamente para ganhar.".

/* let pontuação = prompt ('informe sua pontuação do jogo! ');
if (pontuação >= 100 ){
 alert('Parabéns, você venceu! ');
} else {
    alert ('Tente novamente para ganhar! ');
}
 */

// Crie uma mensagem que informa o usuário sobre o saldo da conta, usando uma template string para incluir o valor do saldo.

/* let informacao = prompt ('informe o saldo que aparece na sua conta! ');

if (informacao>0){
alert (`O saldo da conta é: ${informacao} reais`);
} else if(informacao<0){
    alert (`O saldo da conta é: ${informacao} reais`);
} else{
    alert('informe um valor válido! ');
} */

// Peça ao usuário para inserir seu nome usando prompt. Em seguida, mostre um alerta de boas-vindas usando esse nome. 

/* let nome = prompt ('Informe seu nome: ');

alert (`Seja bem vindo ${nome}`); */

//Crie um programa que utilize o console.log para exibir uma mensagem de boas-vindas. 
/* let nome = prompt ('informe seu nome: ');
console.log ('Bem vindo '+nome); */

/* let nome = prompt ('informe seu nome: ');
console.log (`Bem vindo ${nome}` ); */

//Crie uma variável chamada "nome" e atribua a ela o seu nome. Em seguida, utilize o console.log para exibir a mensagem "Olá, [seu nome]!" no console do navegador. 
/* let nome = prompt ('informe seu nome: ');
console.log (`Bem vindo ${nome}` ); */

//Crie uma variável chamada "nome" e atribua a ela o seu nome. Em seguida, utilize o alert para exibir a mensagem "Olá, [seu nome]!". 
/* let nome = prompt ('informe seu nome: ');
alert (`Bem vindo ${nome}` );
 */

//Utilize o prompt e faça a seguinte pergunta: Qual a linguagem de programação que você mais gosta?. Em seguida, armazene a resposta em uma variável e mostre no console do navegador. 
/* 
let linguagemPreferida = prompt ('Qual a linguagem de programação que você mais gosta?  '); 
console.log (`A linguagem de programação é: ${linguagemPreferida}`);

 */
//Crie uma variável chamada "valor1" e outra chamada "valor2", atribuindo a elas valores numéricos de sua escolha. Em seguida, realize a soma desses dois valores e armazene o resultado 
//em uma terceira variável chamada "resultado". Utilize o console.log para mostrar a mensagem "A soma de [valor1] e [valor2] é igual a [resultado]." no console. 

//parei 
/* let valor1 = prompt ('informe o primeiro valor: ');
let valor2 = prompt ('informe o segundo valor: ');



if((valor1>=0&&valor2>=0)||(valor1<0&&valor2<0)){

soma == valor1+valor2;
console.log('a soma dos dois valores é: '+soma);
} else {
    alert('valor inválido!!! ');
}
 */
//parei aqui dia 29/04 às 17:41

/* let valor1 = prompt('Informe o primeiro valor: ');
let valor2 = prompt('Informe o segundo valor: ');

// Verificar se os valores são numéricos
/* if (isNaN(valor1) || isNaN(valor2)) {
  alert('Um ou ambos os valores são inválidos. Digite apenas números.');
} else {
  // Converter os valores para números (se necessário)
  valor1 = parseFloat(valor1);
  valor2 = parseFloat(valor2);

  // Calcular a soma
  let soma = valor1 + valor2;

  // Exibir a soma
  console.log('A soma dos dois valores é: ' + soma);
} */ 

//Crie uma variável chamada "valor1" e outra chamada "valor2", atribuindo a elas valores numéricos de sua escolha. 
//Em seguida, realize a subtração desses dois valores e armazene o resultado em uma terceira variável chamada "resultado". 
//Utilize o console.log para mostrar a mensagem "A diferença entre [valor1] e [valor2] é igual a [resultado]." no console. 

/* let valor1 = prompt('Informe o primeiro valor! ');
let valor2 = prompt('Informe o segundo valor! ')

if (isNaN(valor1)||isNaN(valor2)){
  alert('informe um valor numérico, esse valor é inválido!! ')

} else {
  valor1 = parseFloat(valor1);
  valor2 = parseFloat(valor2);
  let subtração = valor1-valor2; 
  console.log(`A diferença entre o valor1 e o valor2 é igual a: ${subtração}`);

}  
 */

//Crie uma variável chamada "valor3" e outra chamada "valor4", atribuindo a elas valores numéricos de sua escolha. 
//Em seguida, realize a subtração desses dois valores e armazene o resultado em uma terceira variável chamada "resultado". 
//Utilize o console.log para mostrar a mensagem "A diferença entre [valor3] e [valor4] é igual a [resultado]." no console. 

/* let valor3 = prompt('digite o valor numérico! ');
let valor4 = prompt('digite o valor numérico! ');

if (isNaN(valor3)||isNaN(valor4)){
  alert('não é um valor válido, digite um número!! ');
} else {
  valor3 = parseFloat(valor3);
  valor4 = parseFloat(valor4);
  let subtração = valor3-valor4;
  console.log (`a subtração entre o primeiro numero digitado e o segundo é: ${subtração}`);
} */
//Peça ao usuário para inserir sua idade com prompt. Com base na idade inserida, utilize um if para verificar se a pessoa é maior
//ou menor de idade, exibindo uma mensagem apropriada no console. 

/* let idade = prompt('informe sua idade! ');

if (isNaN(idade)){
  alert ('informe um valor válido!');
} else{
  idade = parseInt(idade);
  if (idade>=18) {
    console.log(`Como sua idade é ${idade} então você é maior de idade!`);
  } else{
    console.log(`como sua idade é ${idade} então você é menor de idade! `);
  }
} */
//caso tenha o html, eu posso fazer assim com o id inputIdade: 
/* const inputIdade = document.getElementById('inputIdade'); // Obter referência ao elemento HTML

function verificarIdade() {
  const idade = parseInt(inputIdade.value); // Obter valor da idade do input

  if (isNaN(idade)) {
    alert('Informe um valor válido!');
    return;
  }

  const mensagem = idade >= 18 ? 
    `Com ${idade} anos, você é maior de idade!` : 
    `Com ${idade} anos, você é menor de idade!`;

  console.log(mensagem); // Exibir mensagem na console
} */

//Crie uma variável "numero" e peça um valor com prompt verifique se é positivo, negativo ou zero. 
//Use if-else para imprimir a respectiva mensagem. 

/* let numero = prompt ('informe um numero!');

if (isNaN(numero)){
  alert('informe um número!!!')
} else {
  if(numero>0){
    alert(`o número ${numero} é positivo! `);
  } else{
    if(numero<0){
      alert(`o número ${numero} é negativo! `);
    } else{
      alert('o número é zero! ')
    }
  } 
} */
//usando operador ternário:
/* let numero = prompt('Informe um número: ');

if (isNaN(numero)) {
  alert('Informe um número válido!');
} else {
  const mensagem = numero > 0 ? `O número ${numero} é positivo!` :
                  numero < 0 ? `O número ${numero} é negativo!` :
                               'O número é zero!';
  alert(mensagem);
} */

//Use um loop while para imprimir os números de 1 a 10 no console.

/* let contador = 1;
while (contador<=10) {

  console.log(contador);
  contador++; 
} */

//Crie uma variável "nota" e atribua um valor numérico a ela. Use if-else para determinar se a nota é maior ou igual a 7 
//e exiba "Aprovado" ou "Reprovado" no console. 
/* 
let nota = prompt ('informe o valor da sua nota! ');

if (isNaN(nota)){
  alert('digite um número válido! ');
} else{
nota = parseInt(nota);
if (nota>=70){
  alert('parabéns, você passou de série! ');
} else {
  alert('infelizmente você não passou de série! Se esforce mais no próximo ano! ');
}
} */

//Use o Math.ramdon para gerar qualquer número aleatório e exiba esse número no console. 

/* let numeroAleatorio = Math.random();
console.log(numeroAleatorio); */

// Use o Math.ramdon para gerar um número inteiro entre 1 e 10 e exiba esse número no console.

/* let numeroInteiro = parseInt(Math.random()*10)+1;
console.log(numeroInteiro); */

//Use o Math.ramdon para gerar um número inteiro entre 1 e 1000 e exiba esse número no console. 

/* let numeroInteiro = parseInt(Math.random()*1000)+1;
console.log(numeroInteiro); */

//calculadora de média de notas

/* let nota1 = prompt ('informa a nota do aluno! ');
let nota2 = prompt ('informa a nota do aluno! ');
let nota3 = prompt ('informa a nota do aluno! ');
let nota4 = prompt ('informa a nota do aluno! ');


if(isNaN(nota1)||isNaN(nota2)||isNaN(nota3)||isNaN(nota4)){
  alert('valor inválido');
}else {
nota1 = parseFloat(nota1);
nota2 = parseFloat(nota2);
nota3 = parseFloat(nota3);
nota4 = parseFloat(nota4);

const media = calcularMedia(nota1, nota2, nota3, nota4);
const resultado = verificarAprovacao(media);
alert(`a mádia do aluno é ${media}`);
alert (resultado);

}

function calcularMedia(nota1, nota2, nota3, nota4) {
  let media = (nota1 + nota2 + nota3 + nota4) / 4;
  media = parseFloat(media);
  return media;
}

function verificarAprovacao(media) {
  return media >= 50 ? "Aprovado" : "Reprovado";
  
}

/* /*  */

//Crie uma função que calcule o índice de massa corporal (IMC) de uma pessoa, a partir de sua altura, em metros, e peso, 
//em quilogramas, que serão recebidos como parâmetro:

/* peso = prompt('informe é peso em kg da pessoa! ');
altura = prompt('informe a altura em metros da pessoa!');

if (isNaN(peso)||isNaN(altura)){
  alert('valor invalido!!! ');
} else{
  peso = parseFloat(peso);
  altura=parseFloat(altura);
  mostrarImc = imcPessoa(peso, altura);
  alert(`o imc da pessoa é: ${mostrarImc}`);

}

function imcPessoa (peso, altura) {
  imc = peso / (altura*altura);
  imc = parseFloat(imc);
  imc = imc.toFixed(2);
  return imc; 
}
*/
//parei às 17:48 do dia 01/05/2024 - exercicio dois. 

//recomecei às 15:30 do dia 04/05

//Crie uma função que calcule o valor do fatorial de um número passado como parâmetro.

/* let numero = prompt ('informe um número para calcular o fatorial!! ');

if (isNaN(numero)){
  alert('digite um numero válido!! ');
}else {
  numero=parseFloat(numero);
  mostrarFatorial = calculaFatorial(numero);
  alert(mostrarFatorial);
}

function calculaFatorial (numero){
  if (numero == 0 || numero == 1) {
    return 1;
  }

  let fatorial = 1;
  for (let i = 2; i <= numero; i++) {
    fatorial *= i;
  }

  return fatorial;
} */

//Crie uma função que converte um valor em dólar, passado como parâmetro, e retorna o valor equivalente em reais. 
//Para isso, considere a cotação do dólar igual a R$ 4,80.

/* let conversao= prompt ('informe o valor em dólares que deseja converter: '); 

if (isNaN(conversao)){
  alert ('digite um valor válido!!!');

} else{
  conversao = parseFloat(conversao);
  mostrandoValor = converterreal(conversao);
  alert ('o valor em reais é: '+mostrandoValor);

}

function converterreal (conversao){
  let valorReal = conversao*4.80;
  return valorReal;
} */
//Crie uma função que mostre na tela a área e o perímetro de uma sala retangular, 
//utilizando altura e largura que serão dadas como parâmetro.
/* 
let lado1 = prompt ('informe o valor do lado 1 ou altura do retângulo!');
let lado2 = prompt ('informe o valor do lado 2 ou a base do retângulo!');

if (isNaN(lado1)||isNaN(lado2)){
  alert('informe um valor válido!!!');
} else {
  lado1 = parseFloat(lado1);
  lado2 = parseFloat(lado2);
  area1 = calcularArea(lado1,lado2); 
  area2 = calcularPerimetro(lado1, lado2);
  alert('o valor da área: '+area1); 
  alert('o valor do perímetro é: '+area2);

}

function calcularArea (lado1, lado2){
  let area = lado1*lado2;
   return area;
}
function calcularPerimetro(lado1, lado2){
  let perimetro = lado1+lado2+lado1+lado2;
  return perimetro;
} */

/* let altura = 3; // em metros
let largura = 5; // em metros
calcularAreaPerimetroSalaRetangular(altura, largura);

function calcularAreaPerimetroSalaRetangular(altura, largura) {
  let area = altura * largura;
  let perimetro = 2 * (altura + largura);
  
  alert(`Área da sala: ${area} metros quadrados`);
  alert(`Perímetro da sala: ${perimetro} metros`);
} */

//Crie uma função que mostre na tela a área e o perímetro de uma sala circular, 
//utilizando seu raio que será fornecido como parâmetro. Considere Pi = 3,14.

/* let raio = prompt('informe o valor do raio do circulo! ');
let pi = 3.14;

if (isNaN(raio)){
  alert ('informe um valor válido! ');

}else {
  mostrarValor(raio,pi);
}

function mostrarValor (raio, pi){
    let area = pi*raio*raio;
    let perimetro = raio*raio;
    alert (`o valor da área é ${area}`);
    alert (`o valor do perimetro é: ${perimetro}`);
} */

//Crie uma função que mostre na tela a tabuada de um número dado como parâmetro.
/* 
let numero = prompt ('informe o número para calcular a tabuada! ');

if (isNaN(numero)){
  alert ('Informe um valor válido!!!');
 } else{
  tabuada (numero);

 }
 function tabuada(numero){
    for (let i = 0; i<=10; i++){
      let resultado = i*numero;
      alert (`${numero} vezes ${i} é ${resultado}`);
      

    } 
      
 }*/

 //Crie uma lista vazia, com o nome listaGenerica.
/*  let listaGenerica = []; 
  */

//Crie uma lista de linguagens de programação chamada linguagensDeProgramacao.

/* let linguagensDeProgramacao = ['java', 'python', 'C++', 'C#', 'javascript' ]; */

//Adicione à lista linguagensDeProgramacao os seguintes elementos: Java, Ruby e GoLang. Mostre como um alerta na tela;

/* let linguagensDeProgramacao = ['java', 'python', 'C++', 'C#', 'javascript' ];
linguagensDeProgramacao.push = ['Ruby', 'Golang'];

alert (linguagensDeProgramacao);
 */

//Crie uma lista com 3 nomes e exiba no console apenas o primeiro elemento.

/* let lista = ['curso1', 'curso2', 'curso3']; 
console.log ('mostrando a lista: '+lista);
console.log ('mostrando o primeiro valor da lista: '+lista[0]); */

//Crie uma lista com 3 nomes e exiba no console apenas o segundo elemento.

/* let lista = ['nome1','nome2','nome3'];

console.log ('mostrando o segundo nome da lista: '+lista[1]); */

//Crie uma lista com 3 nomes e exiba no console apenas o último elemento.
/* let lista = ['nome1', 'nome2', 'nome3'];
console.log('o último nome é: '+lista.length);
 */

//terminei às 17:26 - 04/05